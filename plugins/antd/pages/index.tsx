import React from "react";

import { Header, Main, Cards, Footer } from "@components";

const Home: React.FC = () => {
    return (
        <>
            <Header />
            <Main />
            <Cards />
            <Footer />
        </>
    );
};

export default Home;
